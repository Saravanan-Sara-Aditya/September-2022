import projectData from "./Projects/data";
import React from "react";

function Filter() {
    let otp = projectData.filter((value) => {
        if (value.title === value.title) {
            console.log(otp)
        }
    })
}
export default Filter